0: PacketMotionData
Dados de movimento dos carros (posição, velocidade, aceleração, direção, etc).
1: PacketSessionData
Dados gerais da sessão (clima, tipo de sessão, número de voltas, temperatura, status do safety car, etc).
2: PacketLapData
Dados de volta de cada carro (tempo de volta, setor, posição, número da volta, delta para líder e carro da frente, status de pit, penalidades, etc).
3: PacketEventData
Eventos especiais (volta mais rápida, aposentadoria, penalidade, vencedor, colisão, etc).
4: PacketParticipantsData
Dados dos participantes (nome, número, nacionalidade, equipe, se é IA ou humano, etc).
5: PacketCarSetupData
Setup do carro (asas, cambagem, toe, pressão dos pneus, combustível, etc).
6: PacketCarTelemetryData
Telemetria do carro (velocidade, aceleração, freio, marcha, temperatura dos pneus e freios, pressão dos pneus, etc).
7: PacketCarStatusData
Status do carro (composto do pneu, idade do pneu, combustível, DRS, ERS, modo de combustível, etc).
8: PacketFinalClassificationData
Classificação final da corrida (posição final, voltas completadas, tempo total, penalidades, pneus usados, etc).
9: PacketLobbyInfoData
Informações do lobby (jogadores na sala, status de pronto, equipe, nacionalidade, etc).
10: PacketCarDamageData
Dados de dano do carro (desgaste e dano dos pneus, asa dianteira/traseira, assoalho, difusor, sidepod, falha de DRS/ERS, danos no motor, etc).
11: PacketSessionHistoryData
Histórico da sessão (tempos de volta, setores, stints de pneus, etc).
12: PacketTyreSetsData
Dados dos conjuntos de pneus disponíveis e usados (composto, desgaste, vida útil, etc).
13: PacketMotionExData
Dados extras de movimento (suspensão, forças nas rodas, ângulos, etc).
14: PacketTimeTrialData
Dados de time trial (melhores tempos do jogador, rival, etc).

🧱 Comandos Básicos
Comando	O que faz
plt.plot(x, y)	Gráfico de linha
plt.bar(x, y)	Gráfico de barras
plt.barh(x, y)	Gráfico de barras horizontal
plt.scatter(x, y)	Gráfico de dispersão (pontos)
plt.pie(x)	Gráfico de pizza
plt.hist(x)	Histograma
plt.boxplot(x)	Boxplot (distribuição estatística)

✍️ Anotações e Estilo
Comando	Uso
plt.title("Título")	Adiciona um título ao gráfico
plt.xlabel("Eixo X")	Nomeia o eixo X
plt.ylabel("Eixo Y")	Nomeia o eixo Y
plt.grid(True)	Mostra grade
plt.legend()	Mostra legenda (se tiver labels)
plt.text(x, y, "Texto")	Escreve texto no gráfico
plt.xticks([...])	Define os valores no eixo X
plt.yticks([...])	Define os valores no eixo Y

🎨 Cores e Estilo
Parâmetro	O que faz
color='red'	Cor da linha/barras/pontos
linestyle='--'	Estilo da linha (pontilhada, contínua, etc)
marker='o'	Marcador nos pontos de linha
alpha=0.5	Transparência (0 a 1)
linewidth=2	Espessura da linha

Proximos passos :
faz uma IA rundimenta
🧭 Principais módulos

plotly.express as px — API simples e rápida (recomendada para a maior parte dos gráficos).

plotly.graph_objects as go — construção detalhada por objetos (full control).

plotly.subplots.make_subplots — criar layouts com múltiplos eixos/subplots.

plotly.io as pio — I/O: mostrar, salvar, configurar renderers.

plotly.figure_factory as ff — utilitários e gráficos "pré-montados" (heatmap anotado, tabela, dendrograma).

dash / dash_core_components — integração para apps interativos (se quiser UI).

🧩 Plotly Express (px) — os comandos mais usados

Uso: px.<tipo>(data_frame, x=..., y=..., color=..., facet_row=..., animation_frame=...)

Principais funções:

px.scatter(...)

px.line(...)

px.bar(...)

px.histogram(...)

px.box(...)

px.violin(...)

px.pie(...)

px.density_heatmap(...) / px.density_contour(...)

px.imshow(...) (para imagens / matrizes)

px.scatter_3d(...)

px.line_3d(...) (usar px.scatter_3d mais comum)

px.choropleth(...) / px.choropleth_mapbox(...) (mapas)

px.treemap(...), px.sunburst(...), px.icicle(...) (hierarquias)

px.parallel_coordinates(...), px.parallel_categories(...)

px.sunburst(...), px.sunburst

px.scatter_matrix(...) (pairplot)

Exemplo:

import plotly.express as px
fig = px.scatter(df, x="lap_time", y="speed", color="driver", size="tyre_wear")
fig.show()

🏗️ graph_objects (go) — construindo passo a passo

Você constrói traces e layout explicitamente.

Traces (tipos principais):

go.Scatter (2D lines/markers)

go.Bar

go.Box

go.Violin

go.Histogram

go.Heatmap

go.Contour

go.Surface (3D surface)

go.Mesh3d

go.Scatter3d

go.Pie

go.Choropleth

go.Indicator (gauges/metrics)

go.Table (tabelas)

go.Candlestick (financeiro)

Exemplo:

import plotly.graph_objects as go
fig = go.Figure()
fig.add_trace(go.Scatter(x=x, y=y, mode='lines+markers', name='piloto'))
fig.update_layout(title='Tempo por volta', xaxis_title='Volta', yaxis_title='Tempo (s)')
fig.show()

🔀 Subplots, eixos múltiplos, e grids
from plotly.subplots import make_subplots

fig = make_subplots(rows=2, cols=2, shared_xaxes=True,
                    subplot_titles=("A", "B", "C", "D"),
                    specs=[[{"type":"xy"}, {"type":"xy"}],
                           [{"type":"scene"}, {"type":"domain"}]])  # "scene" para 3D
fig.add_trace(go.Scatter(...), row=1, col=1)
fig.add_trace(go.Bar(...), row=1, col=2)
fig.add_trace(go.Scatter3d(...), row=2, col=1)
fig.add_trace(go.Pie(...), row=2, col=2)
fig.update_layout(height=800, width=1200)
fig.show()

▶️ Animações

No px: animation_frame e animation_group.

fig = px.scatter(df, x="x", y="y", animation_frame="lap", animation_group="driver")
fig.show()


Em go: usar frames e fig.update(frames=...), fig.update_layout(updatemenus=...).

🔁 Atualizar figuras (interatividade programática)

fig.update_traces(...) — altera propriedades dos traces (cor, marker, opacity).

fig.update_layout(...) — altera layout (títulos, legendas, margens).

fig.add_trace(...) / fig.data / fig.data[i] — manipular dados direto.

fig.update_yaxes(...), fig.update_xaxes(...).

Exemplo:

fig.update_traces(marker=dict(size=6), selector=dict(mode='markers'))
fig.update_layout(title_text="Novo título")

💾 Salvar e exportar

fig.show() — mostra no navegador (ou notebook).

pio.write_html(fig, "grafico.html", full_html=True) — salva HTML interativo.

fig.write_html("grafico.html")

fig.write_image("grafico.png") — precisa kaleido (ou orca) instalado.

pio.to_json(fig) / pio.write_json(...) — salvar figura em JSON do Plotly.

🔧 plotly.io & renderers

pio.renderers.default = "notebook" / "browser" / "svg" / "png"

Ex.: pio.renderers.default = "browser"

🧰 figure_factory (ff)

Utilitários e gráficos ajudantes:

ff.create_annotated_heatmap(...)

ff.create_table(...)

ff.create_dendrogram(...)

ff.create_quiver(...) (vetores)
(use docs para funções específicas)

📈 Métricas e indicadores

go.Indicator para KPIs (gauge, number + delta).

fig.add_trace(go.Indicator(...)) — ótimo para dashboards.

🔎 Ferramentas úteis (interação do usuário)

fig.update_layout(hovermode='x unified') — hover unificado.

Ferramentas de zoom, box select, lasso, autoscale.

fig.data[i].selectedpoints — trabalhar com seleção programática.

🧠 Integração com Dash (para UIs interativas)

dash permite callbacks reativos (@app.callback) usando Input, Output, State.

Plotly e Dash são feitos para trabalhar juntos: use dcc.Graph(figure=fig).

🧾 Exemplo mínimo completo (PX + salvar)
import plotly.express as px
df = px.data.iris()
fig = px.scatter(df, x='sepal_length', y='sepal_width', color='species', size='petal_length')
fig.write_html("iris_scatter.html")
fig.show()

🔍 Dicas rápidas / truques

Use px para prototipar rápido; se precisar de ajustes finos, converta para go com fig = px ...; fig.update_traces(...).

Para dados grandes, use scattergl em vez de scatter (go.Scatter(mode='markers', marker={'symbol':'circle'}, ...) com render_mode='webgl' em versões recentes).

Prefira kaleido para exportar imagens (mais simples que orca).

fig.to_dict() devolve a representação serializável (útil para salvar estado).

✅ Referência resumida — Métodos/Comandos mais importantes

px.scatter, px.line, px.bar, px.histogram, px.box, px.imshow, px.choropleth, px.treemap

go.Figure(), go.Scatter, go.Bar, go.Heatmap, go.Scatter3d, go.Surface, go.Indicator

fig.add_trace(), fig.update_traces(), fig.update_layout(), fig.update_xaxes(), fig.update_yaxes()

make_subplots(), ff.create_annotated_heatmap()

fig.show(), fig.write_html(), fig.write_image(), pio.to_json()

animation_frame (px) / frames (go)

pio.renderers.default = 'browser' / 'notebook'